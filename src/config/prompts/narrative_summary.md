Section: Narrative Summary and Analysis

Summarize only the figures provided in the approved evidence packet.
Describe visible patterns in the supplied breakdowns, but do not convert
observations into medical conclusions.
Do not call anything a "signal", "concern", or confirmed causal relationship
unless the supplied evidence explicitly supports that framing.
Cover: total cases, seriousness distribution, top reactions (frame as "Top reported reactions by case-level frequency include...", NEVER "occurrence counts"), patient demographics (age, sex), geographic distribution, and outcome frequencies across reported outcome entries (noting that a single case may contribute multiple outcomes).
CRITICAL RULE: Reaction frequencies represent case-level deduplicated counts (each Preferred Term counted once per case). Never describe reaction frequencies as case-level counts of cases. Outcome frequencies may include multiple entries from the same case.
Target length: 150-250 words.
